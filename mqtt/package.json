{
  "name": "mqtt-api",
  "version": "1.0.0",
  "description": "",
  "main": "mqtt.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "nodemon -r dotenv/config mqtt.js",
    "deploy": "now rm mqtt-217-288-933-sit-209 -y && now -p && now alias"
  },
  "author": "Tim Tattersall",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.18.3",
    "dotenv": "^6.0.0",
    "express": "^4.16.3",
    "mqtt": "^2.18.3",
    "nodemon": "^1.18.3"
  }
}
